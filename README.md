# Dump QGIS project to to GPKG

```
usage: offline.py [-h] db schema project

positional arguments:
  db          DB Name
  schema      DB Schema
  project     Project name

optional arguments:
  -h, --help  show this help message and exit
```